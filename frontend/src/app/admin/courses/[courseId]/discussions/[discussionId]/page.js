// Admin sees the same thread page as students,
// but with moderation controls visible (role-based in the component)
export { default } from "@/app/student/courses/[courseId]/discussions/[discussionId]/page";
